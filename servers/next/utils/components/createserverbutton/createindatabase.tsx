import { Button, ButtonProps } from "@chakra-ui/react";

export default function CreateInDatabase(props: ButtonProps) {
  const handleButton = async () => {};
  return <Button {...props} onClick={() => handleButton}></Button>;
}
