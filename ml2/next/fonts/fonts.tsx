import { Inter, REM, Rubik } from "next/font/google";

export const inter = Inter({ subsets: ["latin"] });

export const rem = REM({ subsets: ["latin"] });

export const rubik = Rubik({ subsets: ["latin"] });
